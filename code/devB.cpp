#include<bits/stdc++.h>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(false); cin.tie(0);cout.tie(0);
	string s;
	while(cin >> s)
	{
		for(int i = 1; i < s.size(); i++)
			cout << 9;
		cout << 8;
		for(int i = 1; i < s.size(); i++)
			cout << 0 ;
		cout << 1 << endl;
	}
	return 0;
}
